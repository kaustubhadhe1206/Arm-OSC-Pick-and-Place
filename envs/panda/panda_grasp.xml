<mujoco model="panda_grasp">
  <!-- pulls in the full real Franka Emika Panda model (joints, links, meshes, actuators) -->
  <include file="panda.xml"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="120" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>

    <!-- fixed scene camera; mode="targetbody" keeps the object in frame
         automatically even though its position is randomized each reset -->
    <camera name="scene_cam" pos="1.0 -0.7 0.9" mode="targetbody" target="object"/>

    <!-- the graspable object: a real physical body (unlike Phase 1's mocap
         "ghost" target), so the gripper can actually push against it and
         hold it via friction/contact. A 3cm cube, small enough to fit
         comfortably within the Panda's ~8cm max finger opening.
         Higher-than-default friction so it doesn't slip out of the grip. -->
    <body name="object" pos="0.4 0 0.015">
      <freejoint/>
      <geom type="box" size="0.015 0.015 0.015" rgba="1 0 0 1" density="500"
        friction="1.5 0.005 0.0001"/>
    </body>
  </worldbody>
</mujoco>
